[pytest]
addopts = -q
filterwarnings = ignore::DeprecationWarning
# Disable auto plugin discovery to avoid web3.tools pytest plugin import error
pythonpath = .
# Clear PYTEST_DISABLE_PLUGIN_AUTOLOAD at runtime via env when invoking if needed
